# Railway Procfile
# Defines how to run the application on Railway

# Start the backend Express server
# The backend will serve on the PORT specified by Railway (default 4000)
web: cd backend && npm run build && npm start
